name: TruffleHog Secrets Scan (Scheduled)

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  trufflehog:
    uses: ./.github/workflows/security-trufflehog.yml
